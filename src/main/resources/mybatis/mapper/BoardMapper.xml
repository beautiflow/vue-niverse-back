<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.vue_niverse.mapper.BoardMapper">
    <insert id="insertBoard" parameterType="com.project.vue_niverse.dto.BoardDto">
        insert into board (title, content, author)
        values (#{title}, #{content}, #{author})
    </insert>

    <select id="findAll" resultType="com.project.vue_niverse.dto.BoardDto">
        select
            id,
            title,
            content,
            author,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM board
        ORDER BY id DESC
    </select>

</mapper>